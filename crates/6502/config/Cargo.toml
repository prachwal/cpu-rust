[package]
name = "mos6502-config"
version = "0.1.0"
edition = "2021"
description = "MOS 6502 CPU emulator configuration types"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
